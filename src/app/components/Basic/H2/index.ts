import styled, { CSSProperties } from 'styled-components';

const H2 = styled.h2<CSSProperties>`
  font-family: 'DMSans';
  font-style: normal;
  font-weight: 700;
  font-size: 22px;
  line-height: 29px;
  color: var(--_primaryTextColor);
  margin: 0;
`;
export default H2;
